# 运维待办

- [x] 后续生产代码发布统一使用 `scripts/deploy-production.sh`，不要再用临时拼接的长 SSH 命令部署。
- [x] 等 SSH 恢复后，为 `www.aigeamy.com` 和 `aigeamy.com` 配置源站 `443` HTTPS 证书，并把 Nginx 切到正式 HTTPS 回源。
- [ ] 源站 HTTPS 验证通过后，提醒将 Cloudflare `SSL/TLS -> Overview` 从临时 `Flexible` 还原为 `Full` 或 `Full strict`。（已验证源站 HTTPS；等待你在 Cloudflare 后台切回。）
- [x] 验证正式站首页邮箱为 `yangdengkui01@gmail.com`，且不再出现 `support@aigeamy.com`。
- [x] 等 SSH 恢复后，删除服务器侧 `test.aigeamy.com` 测试入口：从 `/data/hermes-agent/hermes-agent.env` 的 `APP_ORIGIN` 移除 test 域名，并从 Nginx 配置移除 `test.aigeamy.com` server block。
- [x] 等 SSH 恢复后，将服务器生产环境支付配置切到 Polar：`PAYMENT_PROVIDER=polar`、`POLAR_ENV=live`，并把生产 API key 只写入 `/data/hermes-agent/hermes-agent.env` 后重启 `hermes-agent.service`。
