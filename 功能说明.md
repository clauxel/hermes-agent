# 功能说明：删除实例与白名单 1 美元支付

本文档用于说明当前仓库里两个容易混淆的业务规则：

- 管理员何时能看到并使用 `Delete Hermes`
- 白名单用户何时能按 `$1.00` 支付

## 1. 删除实例的使用条件

### 1.1 只有管理员才可能看到按钮

管理员身份的唯一真源是 `ADMIN_ALLOWED_EMAILS`。

- 邮箱命中 `ADMIN_ALLOWED_EMAILS`
- 用户重新登录后会同步为 `admin`
- 未命中白名单时，即使之前是管理员，下次登录也会自动降回 `operator`

### 1.2 管理员也只能删除自己账号名下的实例

`Delete Hermes` 不是“管理员可删除所有实例”，而是“管理员只能删除自己账号名下的实例”。

后端判定条件是：

- 当前访问者是已登录用户
- 当前访问者角色是 `admin`
- 订单的 `user_id` 必须等于当前管理员自己的 `user.id`
- 该订单当前必须有实例

因此，下面这些情况都不会显示删除按钮：

- 这笔订单属于别的账号
- 这笔订单还没有绑定到当前账号
- 这笔订单当前没有可删的实例

### 1.3 前端还会再加一层显示条件

前端只有在下面条件同时满足时才渲染 `Delete Hermes`：

- 当前账号具备用户管理权限
- 后端返回 `order.canAdminDeleteHermes = true`
- 当前这条管理卡是该订单的最新实例记录
- 当前行确实存在 `claw` 实例

这意味着即使你已经是管理员，只要不是该订单的最新实例，按钮也不会显示。

### 1.4 guest 创建的订单，先绑定账号后才能按“自己的实例”处理

如果订单最早是以 guest 身份创建的，需要先绑定到当前登录账号，后端才会把订单 owner 改成该账号。

注意：

- 实例名是在部署当时生成的
- 如果部署发生在 guest 阶段，实例名可能仍然以 `hermes-guest-...` 开头
- 这不等于它现在仍然是 guest 订单，实际是否属于当前账号，以订单的 `user_id` 为准

### 1.5 删除后会发生什么

管理员删除实例时，删除的是当前订单对应的 Hermes 实例，不是整笔订单。

- 会卸载当前 Hermes 实例
- 订单记录仍然保留
- 删除成功后，这条订单的删除按钮会消失
- 后续如果该订单还有可用部署次数，仍然可以重新部署新的实例

### 1.6 管理员与普通用户在页面展示上的区别

`Console` 页面不是“所有用户看到完全一样的内容”，而是按当前登录角色做展示分层。

#### 管理员会额外看到的内容

- 顶部导航会显示 `Manage users`
- `Console` 页面底部会额外显示两个后台模块：
  - `User Management`
  - `Visitor Analytics`
- `Hermes Management` 列表里，如果某条实例同时满足“属于当前管理员本人 + 当前订单最新实例 + 当前确实存在实例”，还会显示 `Delete Hermes`

#### 普通用户不会看到的内容

- 不会看到顶部的 `Manage users`
- 不会看到 `User Management`
- 不会看到 `Visitor Analytics`
- 不会看到 `Delete Hermes`

#### 双方都会看到的内容

下面这些模块，管理员和普通用户都会看到：

- `Pending Orders`
- `Hermes Management`
- `User Center`
- `Orders`

但“看得到模块”不等于“看到的数据范围一样”。

#### 管理员与普通用户看到的数据范围不同

`/api/console-data` 在后端会按角色返回不同范围的数据：

- 管理员：返回全部订单、全部 Hermes 实例，以及用户列表
- 普通用户：只返回自己账号名下的订单和实例
- 如果普通用户当前浏览器里还带着同一个 `guest_token`，还会额外看到这个 guest 会话下的订单和实例

因此页面上的实际差异是：

- 管理员在 `Pending Orders` 和 `Hermes Management` 中看到的是全局视图
- 普通用户在这两个区域中看到的是“自己的视图”

## 2. 白名单 1 美元支付的使用条件

### 2.1 只在生产环境生效

`$1.00` 特价只在 `production` 生效。

下面这些环境不会触发：

- `development`
- 本地 `npm run dev`
- 任何非生产模式启动

### 2.2 只对已登录白名单用户生效

要命中 `$1.00` 支付，必须同时满足：

- 当前环境是 `production`
- 当前用户已登录
- 当前登录邮箱命中 `ADMIN_ALLOWED_EMAILS`

下面这些情况都不会享受 `$1.00`：

- guest 下单
- 已登录但邮箱不在 `ADMIN_ALLOWED_EMAILS`
- 开发环境下单

### 2.3 只影响新建订单，不会回改旧订单

`$1.00` 的实现是在“创建订单”时把 `amount_cents` 写成 `100`。

因此：

- 新创建的白名单订单会按 `$1.00`
- 已经存在的旧 `pending` 订单会保留它创建当时的金额
- 如果旧订单之前是错误价格，继续支付旧订单不会自动修正

遇到这种情况，应该重新创建一笔新订单再支付。

### 2.4 为什么是 1 美元，不是 0.01 美元

当前生产主支付路径使用的是 Creem。

- Creem 创建商品时要求价格不能低于 `$1.00`
- 因此白名单特价最终落地为 `100` cents，也就是 `$1.00`

## 3. 常见排查

### 3.1 管理员看不到 `Delete Hermes`

优先按下面顺序检查：

1. 当前登录邮箱是否仍在 `ADMIN_ALLOWED_EMAILS`
2. 重新登录后角色是否已经同步为 `admin`
3. 这笔订单是否属于当前登录账号
4. 这条管理卡是否为该订单当前最新实例
5. 当前订单是否真的还有实例存在

### 3.2 白名单账号下单还是原价

优先按下面顺序检查：

1. 当前是不是生产环境
2. 当前是不是已登录状态
3. 登录邮箱是否命中 `ADMIN_ALLOWED_EMAILS`
4. 这笔单是不是新建订单，而不是旧的 pending 订单

## 4. 代码参考

- 订单金额改写逻辑：`server-lib/api/order-routes.mjs`
- 删除按钮显示条件：`src/App.tsx`
- 后端删除权限判定：`server-lib/serialization-helpers.mjs`
- 管理员删除接口：`server-lib/api/admin-routes.mjs`
